server:
  port: 24588
spring:
  application:
    name: choerodon-study-service-24588-pyh
  cloud:
    config:
      failFast: true
      retry:
        maxAttempts: 6
        multiplier: 1.5
        maxInterval: 20008010
      uri: localhost:8010
      enabled: false
management:
  server:
    port: 24589
  endpoints:
    web:
      exposure:
        include: '*'